<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="bottom"
    android:background="@color/colorPrimary">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp">
        <TextView
            android:id="@+id/dialog_title"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="请选择"
            android:gravity="center_vertical"
            android:textSize="16sp"
            android:layout_marginLeft="20dp"
            android:textColor="@color/textWhiteNormal"/>
    </LinearLayout>
    <ListView
        android:id="@+id/item_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@null" />
    <View
        android:id="@+id/item_cancel_line"
        android:layout_width="match_parent"
        android:layout_height="5dp"
        android:background="@color/colorPrimaryLight"/>
    <com.baokaicong.android.bmusic.ui.view.dialog.SheetItem
        android:id="@+id/item_cancel"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        app:itemText="取消"
        app:itemTextColor="@color/textWhiteNormal"
        app:itemIcon="@android:drawable/ic_menu_close_clear_cancel"
        android:background="@color/colorPrimary"
        />
</LinearLayout>